CREATE TABLE "dim_date" ("the_date" TEXT,
                         "year" INTEGER,
                         "month" INTEGER,
                         "day" INTEGER,
                         "week" INTEGER,
                         "day_of_week" INTEGER,
                         "yearmonth" TEXT
                       )
